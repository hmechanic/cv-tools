name: macOS

on:
  push:
    branches:
      - master
  pull_request:
    branches:
      - master

permissions:
  contents: read

jobs:
  build:
    uses: ./.github/workflows/common.yml
    with:
      os: macos-15
