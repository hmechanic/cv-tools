name: Ubuntu

on:
  push:
    branches:
      - main
  pull_request:
    branches:
      - main

permissions:
  contents: read

jobs:
  build:
    uses: ./.github/workflows/common.yml
    with:
      os: ubuntu-24.04
